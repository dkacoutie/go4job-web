# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr
# Reliquats de build : le dossier dist verrouille est renomme de cote au lieu
# d'etre supprime (fichier tenu par le serveur de preview, l'antivirus ou
# l'indexeur Windows). Purement regenerable, ne doit jamais etre suivi.
dist.trash.*

# Local env files (IMPORTANT)
.env
.env.*
!.env.example

# Local data dumps (contain production PII, never commit)
prod_data.sql
*.dump.sql

# OS / system
.DS_Store
Thumbs.db
desktop.ini

# Local config / cache
*.local
.vite
.cache
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Env
.env
.env.*
!.env.example

# Env (secrets)
.env
.env.*
!.env.example


tmp/

# Scratch / outillage IA local (jamais de code applicatif)
outputs/
.tmp-*.mjs
.claude/
.codex/
